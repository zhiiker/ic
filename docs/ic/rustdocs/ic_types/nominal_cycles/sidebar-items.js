initSidebarItems({"struct":[["NominalCycles","Struct to handle cycles that we want to track in metrics. They are maintained as a simple u128. We implement our own arithmetic functions on them so that we can ensure that they never overflow or underflow. A similar struct is provided in the protobuf types. We also provide split into low and high parts as protobuf does not support u128."]]});